# To run this start a debian container
#docker run -ti --rm debian
#->build-essential contains a list of packages which are essential 
#for building Ubuntu packages including gcc compiler, make and ot
#her required tools.

apt-get update
apt-get upgrade -y
apt-get install -y build-essential git
